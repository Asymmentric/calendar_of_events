export enum NotificationType {
  EMAIL = "email",
  PUSH = "push",
  SMS = "sms",
  IN_APP = "in-app",
}

export enum NotificationStatus {
  PENDING = "pending",
  DELIEVERED = "delivered",
  FAILED = "failed",
  REJECTED = "rejected",
}
